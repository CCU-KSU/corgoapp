const Header = ({ label }) => {
    return (
        <>
            <div className="header">
                <h1 className="header-label">{label}</h1>
            </div>
        </>
    );
}
 
export default Header;